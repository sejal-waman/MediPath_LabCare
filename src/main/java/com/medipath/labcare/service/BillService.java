package com.medipath.labcare.service;

import com.medipath.labcare.entity.Bill;

public interface BillService {

    // Method to save a Bill
    void save(Bill bill);

    // You can add additional methods if necessary (like finding bills by patient, etc.)
}
